FROM python:3.12-slim

RUN apt-get update -qq && apt-get install -y -qq postgresql-client && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir schedule

COPY scripts/backup_scheduler.py /backup_scheduler.py

CMD ["python", "/backup_scheduler.py"]
